services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skincare_backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Persist WhatsApp session so you don't need to scan QR on every restart
      - whatsapp_session:/app/whatsapp_session
      # Persist server logs
      - logs:/app/logs
    environment:
      - NODE_ENV=production
      - CHROMIUM_PATH=/usr/bin/chromium

volumes:
  whatsapp_session:
    driver: local
  logs:
    driver: local
