# Workspace Rules

- **Custom Error Handling**: Always use custom error classes (from `utils/customErrors.js`) to the maximum extent possible for throwing or handling exceptions in any new code or updates. Do not use the generic `Error` class for operational or expected application failures.
